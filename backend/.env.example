# Stripe — use Secret key (starts with sk_test_ or sk_live_), NOT mk_
# Get it from https://dashboard.stripe.com/apikeys → "Secret key"
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Site URL for Stripe redirect (no trailing slash)
SITE_URL=http://localhost:3000

# Optional
JWT_SECRET=change-me-in-production
ADMIN_PASSWORD=pocketplanter2026
PORT=3000